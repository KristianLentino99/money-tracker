import { describe, expect, it } from 'vitest';

import NavigationLinks from './navigation-links.vue';

describe('NavigationLinks', () => {
  it('compiles the navigation template', () => {
    expect(NavigationLinks).toBeDefined();
  });
});
